module.exports = {
  
      beforeCreate: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterCreate: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeFind: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterFind: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeUpdate: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterUpdate: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeDestroy: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterDestroy: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeAllScope: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterAllScope: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeExactMatchScope: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterExactMatchScope: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeCountScope: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterCountScope: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      beforeCountExactMatchScope: function() {
      (
function beforeAction(values, done) {
    
    done();
}
).apply(this, arguments);
  },

  
      afterCountExactMatchScope: function() {
      (
function afterAction(models, done) {
    
    done();
}
).apply(this, arguments);
  },

  
};